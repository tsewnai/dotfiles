-- Change the default Omarchy look'n'feel.

-- Slightly larger horizontal gaps (top right bottom left).
hl.config({
  general = {
    gaps_out = { top = 5, right = 10, bottom = 5, left = 10 },
  },
})
